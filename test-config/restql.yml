parameter_templates:
  common_id: &common_id
    name: "id"
    type: "integer"
    required: true
    description: "Item id"

apis:
  - name: "Foo"
    path: "/foo"
    method: "GET"
    description: "Get an item"

  - name: "Bar"
    path: "/bar"
    method: "POST"
    description: "Creates an item"
    payload:
      schema: "payload_schema.json"
    actions:
      - type: "db"
        query: query.sql.tpl
      - type: "db"
        query: query.sql.tpl
